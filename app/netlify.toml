# Build configuration lives in the repo-root netlify.toml (base = "app").
# This file only keeps the SPA fallback so client-side routes resolve.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
